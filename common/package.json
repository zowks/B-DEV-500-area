{
  "name": "area-common",
  "version": "0.0.1",
  "description": "Common files for the Area project",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
